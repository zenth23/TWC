@(Html.Kendo().NumericTextBox()
    .Name("quantity")
    .HtmlAttributes(new { required = "required" })
    .Min(1)
    .Decimals(0)
    .Round(false)
    .Format("{0:N0}")
    .Events(e => e.Change("salesOrders.editor.events.qtyCostOnChange"))
    .Enable(true)
)
